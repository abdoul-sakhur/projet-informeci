# Copy to .env.prod and fill in your real domains before deploying.
# Point both DNS records (A/AAAA) at the VPS's IP before starting the stack,
# otherwise Caddy will fail to obtain Let's Encrypt certificates.

DOMAIN=www.interformci.com
API_DOMAIN=api.interformci.com
